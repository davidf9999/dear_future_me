# .env.prod

# WARNING: THIS FILE IS COMMITTED TO VERSION CONTROL.
# IT MUST NOT CONTAIN ANY SECRETS (API keys, passwords, etc.).
# For local secrets or overrides, use a separate .env file (which is gitignored).
# ------------------------------------------------------------------------------

APP_ENV=production
DFM_API_HOST=0.0.0.0
DFM_API_PORT=8000
DFM_API_URL=http://localhost:8000
STREAMLIT_SERVER_PORT=8501
SQLITE_DB_PATH=./data/prod/database.db
CHROMA_DB_PATH=./data/prod/chroma_db
APP_DEFAULT_LANGUAGE=he
DATABASE_URL=sqlite+aiosqlite:///./data/prod/database.db

# Add any other production-specific settings
