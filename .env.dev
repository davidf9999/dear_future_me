# .env.dev

# WARNING: THIS FILE IS COMMITTED TO VERSION CONTROL.
# IT MUST NOT CONTAIN ANY SECRETS (API keys, passwords, etc.).
# For local secrets or overrides, use a separate .env file (which is gitignored).
# ------------------------------------------------------------------------------

APP_ENV=development
DFM_API_HOST=0.0.0.0
DFM_API_PORT=8001 # Different port for dev API
DFM_API_URL=http://localhost:8001
STREAMLIT_SERVER_PORT=8502 # Different port for dev Streamlit
SQLITE_DB_PATH=./data/dev/database.db
CHROMA_DB_PATH=./data/dev/chroma_db
APP_DEFAULT_LANGUAGE=he
# Add any other development-specific settings
